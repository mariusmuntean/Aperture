<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:aperture="clr-namespace:Aperture;assembly=Aperture"
             mc:Ignorable="d"
             x:Class="Aperture.MainPage">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>

        <aperture:ApertureView x:Name="Aperture"
                               ApertureColor="#BBFFD700" />

        <StackLayout Grid.Row="1">
            <Label HorizontalOptions="Center"
                   VerticalOptions="Center"
                   Text="{Binding Source={x:Reference BladeSizeValue}, Path=Value}"></Label>
            <Slider x:Name="BladeSizeValue"
                    Margin="10"
                    Maximum="1.0"
                    Minimum="0.0"
                    Value="{Binding Source={x:Reference Aperture}, Path=ApertureOpening, Mode=TwoWay}"
                    Visual="Material"/>
        </StackLayout>
    </Grid>
</ContentPage>